- name: Register and add jboss repos for all servers in namespace workloads
  hosts: namespace_workloads
  become: true
  tasks:
    - name: Register to Red Hat
      redhat_subscription:
        state: present
        username: "{{ rhsm_username }}"
        password: "{{ rhsm_password }}"
        auto_attach: true

    - name: Enable jboss repo
      command: 'subscription-manager repos --enable=jb-eap-8.0-for-rhel-9-x86_64-rpms'

    #- name: Enable JBoss EAP 8 repository
    #  ansible.builtin.yum_repository:
    #    name: jb-eap-8.0-for-rhel-9-x86_64-rpms
    #    description: JBoss EAP 8 for RHEL 9
    #    baseurl: https://cdn.redhat.com/content/dist/layered/rhel9/x86_64/jbeap/8.0/os
    #    enabled: true
    #    gpgcheck: true

    #- name: Enable JBoss EAP 8 repository
    #  ansible.builtin.yum_repository:
    #    name: jb-eap-8.0-for-rhel-9-x86_64-rpms
    #    enabled: true